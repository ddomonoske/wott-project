I just need a place to brainstorm
# What happens when we run AeroTest?
I'm okay with there only being one file associated with an aerotest. But then what??
- I want a separate window to open that allows me to view and select data
    - data selection happens by dragging the cursor, but maybe I do it through keyboard first if that's an easier first step
- another button that allows me to calculate the CdA based on each section of data and display it in a table
- table contains:
    - time duration of selection
    - avg speed
    - calculated CdA
    - quality of selection
        - variance?
    